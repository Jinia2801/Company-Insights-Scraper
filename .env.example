# .env.example - Copy this file to .env and fill in values if needed
# Currently this project doesn't require any API keys,
# but you can add settings here in the future.

# Example: override how many pages to scrape
# TOTAL_PAGES=5

# Example: custom output directory
# DATA_DIR=./data
